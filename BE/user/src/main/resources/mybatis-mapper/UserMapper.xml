<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.everyportfolio.user.mapper.UserMapper">

    <select id="selectUserById" resultType="com.everyportfolio.user.model.User">
        SELECT ID
        ,NAME
        ,PASSWORD
        FROM USER
        WHERE ID = #{id}
    </select>

    <insert id="insertUser">
        INSERT INTO USER (ID, NAME, PASSWORD, REFRESH_TOKEN)
        VALUES (#{id}, #{name}, #{password}, NULL)
    </insert>

    <update id="updateUserTokenById">
        UPDATE USER SET REFRESH_TOKEN = #{token, jdbcType=VARCHAR} WHERE ID = #{id}
    </update>

    <select id="selectUserTokenById" resultType="String">
        SELECT REFRESH_TOKEN FROM USER WHERE ID = #{id}
    </select>

    <delete id="deleteUserById">
        DELETE FROM USER WHERE ID = #{id}
    </delete>
</mapper>