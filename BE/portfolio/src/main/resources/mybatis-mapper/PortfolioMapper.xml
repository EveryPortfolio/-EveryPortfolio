<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.everyportfolio.portfolio.mapper.PortfolioMapper">

    <insert id="insertPortfolio">
        INSERT INTO PORTFOLIO (USER_ID, TITLE, CONTENT, TEMPLATE_TYPE)
        VALUES (#{id}, #{title}, #{content}, #{templateType})
    </insert>

</mapper>